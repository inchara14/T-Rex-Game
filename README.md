# T-Rex-Game
A T-Rex game made using OpenGL functions.

To run the file, install Ubuntu.
Open Ubuntu terminal, and change the location to where you're saving the .c file.
Execute the command: 'gcc TRexGame.c -o TRexGame -lGL -lGLU -lglut -lm' and './TRexGame'
